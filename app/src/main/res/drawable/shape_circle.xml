<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadius="0dp"
    android:shape="ring"
    android:thicknessRatio="2"
    android:useLevel="false" >
    <solid android:color="@color/white" />
    <stroke
        android:width="1dp"
        android:color="@color/white" />
    <padding
        android:bottom="@dimen/shapeRoundPadding"
        android:right="@dimen/shapeRoundPadding"
        android:left="@dimen/shapeRoundPadding"
        android:top="@dimen/shapeRoundPadding"/>
</shape>